diff --git a/node_modules/arbundles/build/ar-data-bundle.js b/node_modules/arbundles/build/ar-data-bundle.js
index aadd496..1b046eb 100644
--- a/node_modules/arbundles/build/ar-data-bundle.js
+++ b/node_modules/arbundles/build/ar-data-bundle.js
@@ -21,9 +21,12 @@ async function bundleAndSignData(dataItems, signer) {
         headers.set(header, 64 * index);
         return d.getRaw();
     })).then((a) => {
-        return a.reduce((previousValue, currentValue) => {
-            return buffer_1.Buffer.concat([previousValue, currentValue]);
-        }, buffer_1.Buffer.allocUnsafe(0));
+        // https://github.com/Bundlr-Network/arbundles/issues/15
+        // https://github.com/Bundlr-Network/arbundles/pull/18
+        return buffer_1.Buffer.concat(a);
+        // return a.reduce((previousValue, currentValue) => {
+        //     return buffer_1.Buffer.concat([previousValue, currentValue]);
+        // }, buffer_1.Buffer.allocUnsafe(0));
     });
     const buffer = buffer_1.Buffer.from([
         ...utils_1.longTo32ByteArray(dataItems.length),
